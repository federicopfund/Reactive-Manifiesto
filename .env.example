# Environment Variables for Production Deployment
# Copy this file and configure with your actual values
# DO NOT commit the file with real secrets to git!

# ============================================
# REQUIRED VARIABLES
# ============================================

# Application Secret - Generate with: openssl rand -base64 48
# This is used to sign session cookies and CSRF tokens
APPLICATION_SECRET=changeme_generate_a_secure_random_string_here

# Database URL - Provided automatically by Render/Railway/Fly.io
# Format: postgresql://username:password@hostname:5432/database
DATABASE_URL=postgresql://reactive_user:password@hostname:5432/reactive_manifesto

# ============================================
# OPTIONAL VARIABLES (with defaults)
# ============================================

# HTTP Port (set automatically by most platforms)
# PORT=9000

# Java Memory Settings
JAVA_OPTS=-Xmx512m -Xms256m -XX:MaxMetaspaceSize=256m

# SBT Build Settings
SBT_OPTS=-Xmx1024m -Xms512m

# ============================================
# PLATFORM-SPECIFIC NOTES
# ============================================

# Render.com:
#   - DATABASE_URL is automatically set when you connect a PostgreSQL database
#   - PORT is automatically set
#   - APPLICATION_SECRET should be set to "Generate Value" in the dashboard

# Railway.app:
#   - DATABASE_URL is automatically set as $DATABASE_URL
#   - PORT is automatically set as $PORT
#   - Set APPLICATION_SECRET manually in the Variables tab

# Fly.io:
#   - Use: flyctl secrets set APPLICATION_SECRET=$(openssl rand -base64 48)
#   - Use: flyctl postgres attach <postgres-app-name> to get DATABASE_URL

# ============================================
# HOW TO GENERATE APPLICATION_SECRET
# ============================================

# Option 1 - OpenSSL (recommended):
#   openssl rand -base64 48

# Option 2 - SBT:
#   sbt playGenerateSecret

# Option 3 - Node.js:
#   node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"

# Option 4 - Python:
#   python3 -c "import secrets; print(secrets.token_urlsafe(64))"

# ============================================
# SECURITY REMINDERS
# ============================================

# ✅ DO:
#   - Use a different APPLICATION_SECRET for each environment (dev, staging, prod)
#   - Keep secrets in environment variables, never in code
#   - Use at least 64 random characters for APPLICATION_SECRET
#   - Rotate secrets periodically
#   - Use managed PostgreSQL services with SSL

# ❌ DON'T:
#   - Commit this file with real secrets to git
#   - Share secrets in plain text (Slack, email, etc.)
#   - Use weak or predictable secrets
#   - Reuse secrets across different applications
